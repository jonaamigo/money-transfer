import { TransactionTypeColorPipe } from './transaction-type-color.pipe';

describe('TransactionTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
